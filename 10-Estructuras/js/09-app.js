const autenticado = false;
const puedePagar = true;

console.log( autenticado ? puedePagar ? 'Si esta autenticado y puede pagar' : 'Si esta autenticado pero no puede pagar' : 
'no esta autenticado');

